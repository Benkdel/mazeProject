#ifndef KEYBOARD_H
#define KEYBOARD_H

#include "headers.h"
#include "simpMath.h"

typedef struct keyboard_handler {
    Uint8 *currentKeyState;
    vec3 pos;

} keyboard_handler;

/* prototypes */

#endif
